
void FindWinnerEucledian_GPU(struct Node*,struct Winner*);
void GaussianAdapt_GPU(struct Node *node, struct Winner *winner, FLOAT radius, FLOAT alpha);

int TrainMap_GPU(struct Parameters *parameters);

